# OSINT Agent Core Dependencies
pyyaml>=6.0
litellm>=1.60
python-dotenv>=1.0
PySide6>=6.7
gravis==0.1.0
setuptools<81

# Optional — install for specific modules
aiohttp>=3.8
dnspython>=2.0    # DNS queries via Python (fallback if dig not available)
python-whois>=0.8 # WHOIS lookups (fallback)
beautifulsoup4>=4.11 # HTML parsing for advanced scraping
lxml>=4.9         # XML/HTML parsing
playwright>=1.45  # Browser-rendered crawl (also run: playwright install chromium)
jarm       # TLS fingerprinting
